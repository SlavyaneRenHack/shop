# Используйте официальный образ Node.js
FROM node:14

# Создайте директорию для вашего приложения
WORKDIR /usr/src/app

# Копируйте файлы проекта в рабочую директорию внутри контейнера
COPY . .

# Установите зависимости проекта
RUN npm install

# Экспозируйте порт, который слушает ваш сервер
EXPOSE 8001

# Команда для запуска вашего приложения
CMD [ "node", "server.js" ]
